import { describe, it, expect } from 'vitest'

// Placeholder proving the Vitest runner is configured (Issue #1, §7.1).
// Real UI tests (heading renders, loading/error states) arrive in Issue #4.
describe('vitest configuration', () => {
  it('runs a passing test', () => {
    expect(true).toBe(true)
  })
})
